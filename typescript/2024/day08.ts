import { Day } from "../program";

export class Day08 implements Day {
  partOneTestAnswer = 14;
  partTwoTestAnswer = 11387;
  inProgress = true;

  executePartOne(input: string[]): number {
    return input.length;
  }

  executePartTwo(input: string[]): number {
    return input.length;
  }

  createAntinodes(input: string[]) {
    return input;
  }
}
